version: "1.0"
requires:
  framework: ["3.7", "3.8", "3.9"]
  jsModule: [standard, mobile]
attributes:
  hide_agent_responses:
    name: hide_agent_responses
    type: boolean
    description: Hides non customer entries into the thread
    default: true
    required: false
extends:
  widget: standard/output/IncidentThreadDisplay
  components: []
  overrideViewAndLogic: "true"
